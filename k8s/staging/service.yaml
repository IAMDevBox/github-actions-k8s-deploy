apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: staging
  labels:
    app: myapp
    environment: staging
spec:
  selector:
    app: myapp
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: ClusterIP
